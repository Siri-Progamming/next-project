import React from "react";

interface SwitchProps{
    elements: Array<string>;
}
const Switch: React.FC<SwitchProps> = ({elements}) => {
    return (
        <div>
            Switch
        </div>
    )
}
export default Switch;
